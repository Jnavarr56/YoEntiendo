<%= link_to('Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to log out? You will lose unsaved work.' }) %>

<h1>New Annotation</h1>

<%= render 'form', annotation: @annotation %>

<%= link_to 'Cancel', passages_path, data: { confirm: 'Are you sure you want to cancel? You will lose unsaved work.' }%> 

