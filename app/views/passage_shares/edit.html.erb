<%= link_to('Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to log out? You will lose unsaved work.' }) %>
<h1>Editing Passage Share</h1>

<%= render 'form', passage_share: @passage_share %>

<%= link_to 'Cancel', passage_share_path(@passage_share), data: { confirm: 'Are you sure you want to cancel? You will lose unsaved work.' }%> 